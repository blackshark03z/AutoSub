# Legacy PREP00 Templates

These files were active during PREP00 and were superseded by PREP01.

Superseded secret input formats:

- `elevenlabs_keys.example.json`
- `translation_provider.env.example`

Moved duplicate examples:

- root-level PREP00 duplicates
- former `templates\overnight_inputs\` secret templates
- PREP00 root example duplicates for `run_config.example.json` and `source_provenance.example.json`

PREP01 active canonical templates now live only under `templates\overnight_inputs\` and use:

- `elevenlabs_api.example.txt`
- `gemini_api.example.txt`
- `translation_config.env.example`
