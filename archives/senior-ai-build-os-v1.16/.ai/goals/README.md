# Goal records

Each Goal gets `.ai/goals/<goal-id>/plan.json`, `decisions.jsonl`, and on successful final acceptance `result.json`.

Task evidence remains under `.ai/evidence/`; Goal records aggregate orchestration state rather than replacing task integrity evidence.
