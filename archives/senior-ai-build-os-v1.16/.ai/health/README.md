# Codebase Health Runtime

`CODEBASE_HEALTH.json` is generated by `ai_os.py init` / `health baseline` and updated after accepted non-Goal tasks or Goal completion. It is a ratchet baseline, not a demand to clean legacy debt before shipping.
